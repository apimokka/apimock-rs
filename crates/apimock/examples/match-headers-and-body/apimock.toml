# Matching on request headers and body content, layered by
# specificity. Uses the default `first_match` strategy, so rule order
# matters - see apimock-rule-set.toml.

[listener]
ip_address = "127.0.0.1"
port = 3001

[service]
rule_sets = ["apimock-rule-set.toml"]
fallback_respond_dir = "."
