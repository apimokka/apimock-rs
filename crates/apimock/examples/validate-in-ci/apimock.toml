# A config to check with `apimock validate` - no server needs to run
# for either command demonstrated in this set's README.

[listener]
ip_address = "127.0.0.1"
port = 3001

[service]
rule_sets = ["apimock-rule-set.toml"]
fallback_respond_dir = "."
